# 游戏难度机制与配置文件实现计划

## 一、现有代码摸底

### 1.1 当前平衡参数分布

| 参数 | Player 默认值 | Enemy 默认值 | 来源 |
|------|-------------|-------------|------|
| hp | 100 | 10 | 构造参数，完整链路 |
| attackDamage | 15 | 10 | **字段直接初始化，构造函数不传** |
| damageVariance | 5 | 3 | **字段直接初始化，构造函数不传** |
| sightRange | 10 | 7 | 构造参数 |
| moveInterval | -- | 8 (2-arg) / 构造传入 | 构造参数 |
| maxCharge | 100 | -- | 字段初始化 |
| chargeRate | 2 | -- | 字段初始化 |
| enemyBaseCount | -- | 3+random(3) | Enemy.spawnEnemies() 内部硬编码 |

### 1.2 现存代码问题（本次必须一并修复）

**问题A**：[AttackAction.java#L108-L119](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/AttackAction.java#L108-L119)
`calculateDamage()` 完全硬编码伤害值（Player=15±5, Enemy=10±3），根本不调用 `entity.getAttackDamage()` / `entity.getDamageVariance()`。**即使给 Player/Enemy 加了配置化属性，攻击伤害也拿不到。** 这是本次改动的前提修复。

**问题B**：[Enemy.java#L26-L28](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Enemy.java#L26-L28)
2参构造调用6参构造，6参构造签名只有 `(Position, TETile, int hp, int sightRange, int moveInterval, Random)`，**没有 attackDamage 和 damageVariance 参数**。两个字段靠字段初始化设默认值 10 和 3。

**问题C**：[EntityState.java#L10-L13](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/EntityState.java#L10-L13)
存档快照只存 `hp` 和 `sightRange`，不存 `attackDamage`、`damageVariance`、`moveInterval`。读档时：

- [Game.java#L623](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L623)：Player 用 `new Player(pos, s.hp, s.sightRange)` — attackDamage/damageVariance/charge 全用默认值
- [Game.java#L627-L628](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L627-L628)：Enemy 用硬编码 `moveInterval=5` + 默认 attackDamage/damageVariance

**问题D**：[Game.java#L277](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L277)
UI 栏 HP 显示 `"HP: %d/100"` — 分母硬编码为 100。

### 1.3 调用链路追踪

新游戏创建实体的调用链：
```
Game.processInput('s') 
  → spawnPlayer(seed, floor)       // new Player() → hp=100, sightRange=10
  → Enemy.spawnEnemies(world, seed, playerPos, extraCount)
      → new Enemy(new Position(0,0), random)  // 2参构造 → hp=10, sightRange=7, moveInterval=8
```

读档恢复实体的调用链：
```
Game.loadGameState()
  → new Player(new Position(x,y), s.hp, s.sightRange)  // 3参构造，attackDamage=15(默认)
  → new Enemy(pos, tile, s.hp, s.sightRange, 5, random)  // moveInterval=5硬编码
```

---

## 二、文件修改清单与改动细节

### 2.1 新建文件

#### `byog/Core/Difficulty.java`

简单枚举，提供 key 到 Difficulty 的互转：

```java
package byog.Core;

public enum Difficulty {
    EASY("easy"),
    BALANCED("balanced"),
    HARDCORE("hardcore");

    private final String key;

    Difficulty(String key) { this.key = key; }
    public String getKey() { return key; }

    /** 从配置 key 解析，未知 key 默认返回 BALANCED */
    public static Difficulty fromKey(String key) {
        for (Difficulty d : values()) {
            if (d.key.equalsIgnoreCase(key)) return d;
        }
        return BALANCED;
    }
}
```

#### `byog/Core/GameConfig.java`

配置管理类，职责：
1. 构造函数接收 `Difficulty`，调用 `load()`
2. `load()` 从 `config/game.properties` 读配置；文件不存在则自动生成默认文件
3. 每个 getter 返回对应属性值
4. 使用 `java.util.Properties`

字段列表（全公开 final）：

| 字段 | 类型 | 含义 |
|------|------|------|
| difficulty | Difficulty | 当前难度 |
| playerHp | int | 玩家初始血量 |
| playerAttack | int | 玩家基础攻击力 |
| playerDamageVariance | int | 玩家伤害波动范围 |
| playerMaxCharge | int | 蓄力上限 |
| playerChargeRate | int | 每帧蓄力恢复量 |
| enemyHp | int | 敌人基础血量 |
| enemyAttack | int | 敌人基础攻击力 |
| enemyDamageVariance | int | 敌人伤害波动范围 |
| enemyMoveInterval | int | 敌人移动间隔(帧) |
| enemySightRange | int | 敌人视野(曼哈顿距离) |
| enemyBaseCount | int | 每层基础敌人数量 |

关键方法：
```java
public GameConfig(Difficulty difficulty)  // 构造即加载
private void load()                       // 读配置文件 or 生成默认
private void loadDefaults()               // 硬编码默认值（作为无配置文件时的 fallback）
public void generateDefaultConfigFile()   // 写出 config/game.properties
```

配置文件的 key 命名：`{difficulty}.{entity}.{property}`，例：`easy.player.hp=150`

#### `config/game.properties`（自动生成，非手写）

```properties
# Generated by GameConfig - edit to customize difficulty balance
easy.player.hp=150
easy.player.attack=12
easy.player.damageVariance=3
easy.player.maxCharge=80
easy.player.chargeRate=3
easy.enemy.hp=15
easy.enemy.attack=5
easy.enemy.damageVariance=2
easy.enemy.moveInterval=10
easy.enemy.sightRange=5
easy.enemy.baseCount=2

balanced.player.hp=100
balanced.player.attack=15
balanced.player.damageVariance=5
balanced.player.maxCharge=100
balanced.player.chargeRate=2
balanced.enemy.hp=20
balanced.enemy.attack=10
balanced.enemy.damageVariance=3
balanced.enemy.moveInterval=5
balanced.enemy.sightRange=7
balanced.enemy.baseCount=3

hardcore.player.hp=50
hardcore.player.attack=10
hardcore.player.damageVariance=8
hardcore.player.maxCharge=150
hardcore.player.chargeRate=1
hardcore.enemy.hp=30
hardcore.enemy.attack=15
hardcore.enemy.damageVariance=5
hardcore.enemy.moveInterval=3
hardcore.enemy.sightRange=9
hardcore.enemy.baseCount=4
```

---

### 2.2 修改文件

#### `AttackAction.java` — 修复硬编码伤害（前提 Bug 修复）

**当前代码**（[L108-L119](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/AttackAction.java#L108-L119)）：

```java
private int calculateDamage(Entity entity) {
    int baseDamage;
    int variance;
    if (entity instanceof Player) {
        baseDamage = 15;   // ← 硬编码
        variance = 5;      // ← 硬编码
    } else if (entity instanceof Enemy) {
        baseDamage = 10;
        variance = 3;
    } else { ... }
    return baseDamage + uniform(random, variance + 1);
}
```

**改为**：使用实体自身的 getter：

```java
private int calculateDamage(Entity entity) {
    int baseDamage = 5;
    int variance = 2;
    if (entity instanceof Player player) {
        baseDamage = player.getAttackDamage();
        variance = player.getDamageVariance();
    } else if (entity instanceof Enemy enemy) {
        baseDamage = enemy.getAttackDamage();
        variance = enemy.getDamageVariance();
    }
    return baseDamage + uniform(random, variance + 1);
}
```

#### `Player.java` — 添加配置化构造函数

**当前构造函数级联**：

| 构造函数 | 调用 | 默认值 |
|----------|------|--------|
| `Player()` | `this(new Position(0,0), 100, 10)` | hp=100, sight=10 |
| `Player(Position)` | `this(pos, 100, 10)` | hp=100, sight=10 |
| `Player(Position, hp, sightRange)` | 主构造 | -- |

**改动**：保留现有3个构造函数不变（向后兼容），新增一个全参构造：

```java
/** 从 GameConfig 创建玩家 */
public Player(Position position, GameConfig config) {
    super(position, Tileset.PLAYER);
    this.hp = config.playerHp;
    this.sightRange = config.playerSightRange;  // 如果config没有此项则保持10
    this.attackDamage = config.playerAttack;
    this.damageVariance = config.playerDamageVariance;
    this.maxCharge = config.playerMaxCharge;
    this.chargeRate = config.playerChargeRate;
}
```

> 注意：检查是否需要 `playerSightRange` 配置项。当前 Player 有 sightRange 字段但在游戏中未使用。**本次不加此配置项**，保持 sightRange 为默认值 10，避免 scope creep。后续若做 FOV 系统时再加。

#### `Enemy.java` — 扩展构造函数签名 + 修改 spawnEnemies

**当前构造函数级联**：

| 构造函数 | 参数 | 调用 |
|----------|------|------|
| 2参 | `(Position, Random)` | `this(pos, ENEMY, 10, 7, 8, random)` |
| 6参 | `(Position, TETile, hp, sightRange, moveInterval, Random)` | 主构造 |

**改动1**：扩展主构造为8参，加入 `attackDamage` 和 `damageVariance`：

```java
public Enemy(Position position, TETile tile, int hp, int sightRange,
             int moveInterval, int attackDamage, int damageVariance, Random random) {
    super(position, tile);
    this.hp = hp;
    this.sightRange = sightRange;
    this.moveInterval = moveInterval;
    this.attackDamage = attackDamage;
    this.damageVariance = damageVariance;
    this.tickCounter = 0;
    this.random = random;
    this.actionQueue = new ActionQueue();
    this.brain = new RuleBasedBrain(sightRange, random);
}
```

**改动2**：删除2参构造（不再需要简洁默认构造），或保留为快捷构造但使用默认值委托。建议**删除**，因为所有调用方都应显式传入配置参数。

**改动3**：`spawnEnemies()` 新增 `GameConfig` 参数：

```java
public static List<Enemy> spawnEnemies(TETile[][] world, String seed,
        Position playerPos, int extraCount, GameConfig config) {
    List<Enemy> enemies = new ArrayList<>();
    Random countRandom = new Random((seed + "_enemy_count").hashCode());
    int count = config.enemyBaseCount + extraCount + countRandom.nextInt(3);

    for (int i = 0; i < count; i++) {
        Random random = new Random((seed + "_enemy_" + i).hashCode());
        Enemy enemy = new Enemy(new Position(0, 0), Tileset.ENEMY,
                config.enemyHp, config.enemySightRange, config.enemyMoveInterval,
                config.enemyAttack, config.enemyDamageVariance, random);
        Entity.initEntity(enemy, world, seed + "_pos_" + i);

        while (MathHelper.manhattanDistance(enemy.getPosition(), playerPos) < 5) {
            Entity.initEntity(enemy, world, seed + "_pos_" + i + "_retry");
        }
        enemies.add(enemy);
    }
    return enemies;
}
```

#### `EntityState.java` — 扩展可序列化字段

新增字段（all public for serialization）：

```java
public int attackDamage;
public int damageVariance;
public int moveInterval;  // 仅 Enemy 使用，Player 为 0
public int maxCharge;     // 仅 Player 使用，Enemy 为 0
public int chargeRate;    // 仅 Player 使用，Enemy 为 0
```

#### `Game.java` — 核心变更（最多改动）

**2.2.5.1 新增字段**

```java
private Difficulty difficulty;
private GameConfig gameConfig;
```

**2.2.5.2 新增 GameState：DIFFICULTY_SELECT**

```java
private enum GameState {
    MENU, DIFFICULTY_SELECT, SEED_INPUT, PLAYING, PAUSED, QUIT_PENDING, QUIT
}
```

**2.2.5.3 processInput() 扩展**

- MENU 中 `'n'` → `DIFFICULTY_SELECT`（原为 `SEED_INPUT`）
- 新增 `DIFFICULTY_SELECT` case：
  - `'1'` → `difficulty = EASY; gameConfig = new GameConfig(EASY); return SEED_INPUT`
  - `'2'` → `difficulty = BALANCED; gameConfig = new GameConfig(BALANCED); return SEED_INPUT`
  - `'3'` → `difficulty = HARDCORE; gameConfig = new GameConfig(HARDCORE); return SEED_INPUT`
- SEED_INPUT 中 `'s'` → 使用 `gameConfig` 创建 Player/Enemy（见下节）

**2.2.5.4 修改 spawnPlayer()**

```java
private Player spawnPlayer(String seed, int floor) {
    Player p = new Player(new Position(0, 0), gameConfig);
    Player.initPlayer(p, world, seed + "_F" + floor);
    return p;
}
```

**2.2.5.5 修改 Enemy 生成调用点**

SEED_INPUT 的 `'s'` 分支中（[L158-L162](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L158-L162)）：

```java
List<Enemy> enemies = Enemy.spawnEnemies(world, this.seed, player.getPosition(),
        floorLevel - 1, gameConfig);
```

`nextFloor()` 中（[L548](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L548)）：

```java
List<Enemy> enemies = Enemy.spawnEnemies(world, this.seed, player.getPosition(),
        floorLevel - 1, gameConfig);
```

**2.2.5.6 修改存档 saveGameState()**

`EntityState` 序列化时新增字段：

```java
if (e instanceof Player pl) {
    s.type = "Player";
    s.hp = pl.getHp();
    s.sightRange = pl.getSightRange();
    s.attackDamage = pl.getAttackDamage();
    s.damageVariance = pl.getDamageVariance();
    s.maxCharge = pl.getMaxCharge();
    s.chargeRate = pl.getChargeRate();
} else if (e instanceof Enemy enemy) {
    s.type = "Enemy";
    s.hp = enemy.getHp();
    s.sightRange = enemy.getSightRange();
    s.attackDamage = enemy.getAttackDamage();
    s.damageVariance = enemy.getDamageVariance();
    s.moveInterval = enemy.getMoveInterval();  // 需新增 getter
}
data.extraData.put("difficulty", difficulty.getKey());
```

**2.2.5.7 修改读档 loadGameState()**

从存档恢复 difficulty 和 gameConfig，然后用 config 参数重建实体：

```java
// 恢复难度（旧存档无此字段默认 BALANCED）
String savedDifficulty = (String) data.extraData.get("difficulty");
difficulty = Difficulty.fromKey(savedDifficulty);
gameConfig = new GameConfig(difficulty);

// 恢复实体（使用存档中保存的属性值，而非当前配置文件）
...
if ("Player".equals(s.type)) {
    player = new Player(new Position(s.x, s.y), s.hp, s.sightRange);
    // 使用存档保存的攻击属性覆盖默认值
    player.setAttackDamage(s.attackDamage);    // 需新增 setter
    player.setDamageVariance(s.damageVariance); // 需新增 setter
    player.setMaxCharge(s.maxCharge);           // 需新增 setter
    player.setChargeRate(s.chargeRate);         // 需新增 setter
    e = player;
} else if ("Enemy".equals(s.type)) {
    Random random = new Random(data.seed.hashCode());
    Enemy enemy = new Enemy(new Position(s.x, s.y), Tileset.ENEMY,
            s.hp, s.sightRange,
            s.moveInterval != 0 ? s.moveInterval : 5,  // 旧存档兼容
            s.attackDamage != 0 ? s.attackDamage : gameConfig.enemyAttack,  // 旧存档兼容
            s.damageVariance != 0 ? s.damageVariance : gameConfig.enemyDamageVariance,  // 旧存档兼容
            random);
    e = enemy;
}
```

> 兼容策略：旧存档 EntityState 无反序列化新字段时，它们为 Java 默认值 0。用 0 检测 + fallback 到 gameConfig 处理。

**2.2.5.8 修改 UI HP 显示**

[Game.java#L277](file:///d:/Courses/cs61b/cs61b-std/proj2/byog/Core/Game.java#L277)：

```java
// 之前：String.format("HP: %d/100", player.getHp())
StdDraw.text(5, barY, String.format("HP: %d/%d", player.getHp(), gameConfig.playerHp));
```

同时也在 UI 栏显示当前难度：

```java
StdDraw.setPenColor(getDifficultyColor());
StdDraw.text(12, barY, difficulty.getKey().toUpperCase());
```

**2.2.5.9 新增 drawDifficultySelect()**

```java
private void drawDifficultySelect() {
    StdDraw.clear(StdDraw.BLACK);
    StdDraw.setPenColor(StdDraw.WHITE);
    StdDraw.text(WIDTH / 2.0, HEIGHT / 2.0 + 3, "Select Difficulty");
    StdDraw.setPenColor(new Color(100, 200, 100));
    StdDraw.text(WIDTH / 2.0, HEIGHT / 2.0 + 1, "1 - Easy (E)");
    StdDraw.setPenColor(new Color(200, 200, 100));
    StdDraw.text(WIDTH / 2.0, HEIGHT / 2.0, "2 - Balanced (B)");
    StdDraw.setPenColor(new Color(200, 100, 100));
    StdDraw.text(WIDTH / 2.0, HEIGHT / 2.0 - 1, "3 - Hardcore (H)");
}
```

draw() 中增加 DIFFICULTY_SELECT 分支。

**2.2.5.10 playWithInputString 适配**

格式：`n{难度数字}{种子}s`，难度为 `[123]` 中一位数字。

```java
// 新格式: n1123s → 简单 + 种子123
// 兼容旧格式: n123s → 在DIFFICULTY_SELECT状态下，非123的字符默认选BALANCED
```

**2.2.5.11 initKeyBindings（可选）**

DIFFICULTY_SELECT 状态的按键通过 processInput 直接处理（字符 '1','2','3'），不需要 keyBindings。

---

### 2.3 需要新增的 getter/setter

**Player.java**：
- 新增 `setAttackDamage(int)`, `setDamageVariance(int)`, `setMaxCharge(int)`, `setChargeRate(int)` — 用于读档恢复
- `getMaxCharge()`, `getChargeRate()` 已存在

**Enemy.java**：
- 新增 `getMoveInterval()` — 用于存档序列化

---

## 三、状态机流程

```
MENU
  ├── 'n' → DIFFICULTY_SELECT
  ├── 'l' → loadGameState() → PLAYING (从存档恢复 difficulty/config)
  └── 'q' → QUIT

DIFFICULTY_SELECT
  ├── '1' → set EASY    → SEED_INPUT
  ├── '2' → set BALANCED → SEED_INPUT
  └── '3' → set HARDCORE → SEED_INPUT

SEED_INPUT
  └── 's' → generateWorld() + spawnPlayer(config) + spawnEnemies(config) → PLAYING
```

---

## 四、修改汇总

| 文件 | 操作 | 要点 |
|------|------|------|
| `byog/Core/Difficulty.java` | **新建** | 3值枚举 + fromKey() |
| `byog/Core/GameConfig.java` | **新建** | Properties 加载 + 默认值 + 自动生成配置文件 |
| `byog/Core/AttackAction.java` | **修改** | `calculateDamage()` 改用 entity getter，不再硬编码 |
| `byog/Core/Enemy.java` | **修改** | 构造扩至8参(+attackDamage,+damageVariance)；spawnEnemies 加 GameConfig 参数；新增 getMoveInterval() |
| `byog/Core/EntityState.java` | **修改** | 新增 attackDamage, damageVariance, moveInterval, maxCharge, chargeRate 字段 |
| `byog/Core/Player.java` | **修改** | 新增4个 setter（读档用）；新增 GameConfig 构造函数 |
| `byog/Core/Game.java` | **修改** | 状态机加 DIFFICULTY_SELECT；新增 difficulty/gameConfig 字段；spawnPlayer/spawnEnemies 传 config；存档/读档适配；UI 适配 |
| `config/game.properties` | **自动生成** | 首次运行 GameConfig 构造时生成 |

**编译命令需更新**（新增 Difficulty.java 和 GameConfig.java）：

```powershell
javac -d out/production/proj2 -cp "D:\Courses\cs61b\cs61b-std\library-sp18\avalib\stdlib-package.jar;D:\Courses\cs61b\cs61b-std\library-sp18\avalib\algs4.jar;D:\Courses\cs61b\cs61b-std\library-sp18\avalib\junit-4.12.jar;D:\Courses\cs61b\cs61b-std\library-sp18\avalib\hamcrest-core-1.3.jar;D:\Courses\cs61b\cs61b-std\library-sp18\avalib\jh61b.jar" byog/Core/*.java byog/TileEngine/*.java byog/Helper/*.java byog/lab5/Position.java
```
（`byog/Core/*.java` 已涵盖新文件）

---

## 五、风险与边界条件

| 场景 | 处理 |
|------|------|
| 配置文件不存在 | GameConfig.load() → loadDefaults() → generateDefaultConfigFile() |
| 配置文件格式错误/值非法 | Properties 读取 + try-catch NumberFormatException → 该字段用默认值 |
| 旧存档无 difficulty 字段 | `Difficulty.fromKey(null)` 返回 BALANCED |
| 旧存档 EntityState 无新字段 | 新字段为默认值 0 → saveGameState 中用 `!= 0` 判断 + fallback |
| playWithInputString 旧格式 `n123s` | DIFFICULTY_SELECT 状态下非123字符 → 默认选 BALANCED |
| 难度选择后按 's' 无种子 | SEED_INPUT 保持空种子逻辑不变 |
